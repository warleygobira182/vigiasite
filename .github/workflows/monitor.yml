name: 🚀 Monitoramento Automático VigiaSite

on:
  schedule:
    - cron: '*/10 * * * *'  # A cada 10 minutos
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🕐 Acionar verificação automática
      run: |
        echo "🚀 Executando verificação automática..."
        response=$(curl -s -X POST "https://vigiasite.vercel.app/api/monitor.js" \
          -H "Content-Type: application/json" \
          -d '{"action": "cron-job"}')
        echo "Resposta: $response"
        echo "✅ Verificação concluída!"
